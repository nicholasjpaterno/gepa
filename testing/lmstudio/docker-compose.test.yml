version: '3.8'

services:
  gepa-lmstudio-test:
    build:
      context: .
      dockerfile: Dockerfile.test
    network_mode: host
    environment:
      - LMSTUDIO_URL=http://localhost:1234
    volumes:
      - ./test_results:/app/results
    command: python test_lmstudio.py